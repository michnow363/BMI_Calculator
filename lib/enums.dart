
enum ValueType {
  height,
  weight,
}

enum UnitSystem {
  metric,
  imperial,
}

enum HeightUnit {
  m,
  feet
}

enum WeightUnit {
  kg,
  lb,
}

enum BmiLevel {
  starvation,
  emaciation,
  underweight,
  correct,
  overweight,
  obesityI,
  obesityII,
  obesityIII,
  empty
}