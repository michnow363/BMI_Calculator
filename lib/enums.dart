
enum ValueType {
  height,
  weight,
}

enum UnitSystem {
  metric,
  imperial,
  oldPolish,
}

enum HeightUnit {
  m,
  foot,
  lokiec,
}

enum WeightUnit {
  kg,
  lb,
  funt,
}

enum BmiLevel {
  starvation,
  emaciation,
  underweight,
  correct,
  overweight,
  obesityI,
  obesityII,
  obesityIII,
  empty
}